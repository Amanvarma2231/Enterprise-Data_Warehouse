```mermaid
erDiagram
    stg_customers {
        VARCHAR customer_id
        VARCHAR first_name
        VARCHAR last_name
        VARCHAR email
        VARCHAR phone
        VARCHAR city
        VARCHAR state
        VARCHAR country
        VARCHAR postal_code
        VARCHAR segment
        VARCHAR registration_date
        VARCHAR is_active
        TIMESTAMP _ingested_at
        VARCHAR _source_file
    }

    stg_products {
        VARCHAR product_id
        VARCHAR sku
        VARCHAR product_name
        VARCHAR category
        VARCHAR subcategory
        VARCHAR unit_cost
        VARCHAR unit_price
        VARCHAR reorder_level
        VARCHAR is_discontinued
        TIMESTAMP _ingested_at
        VARCHAR _source_file
    }

    stg_stores {
        VARCHAR store_id
        VARCHAR store_name
        VARCHAR store_type
        VARCHAR region
        VARCHAR city
        VARCHAR state
        VARCHAR square_feet
        VARCHAR opened_date
        VARCHAR manager_name
        TIMESTAMP _ingested_at
        VARCHAR _source_file
    }

    stg_orders {
        VARCHAR order_id
        VARCHAR customer_id
        VARCHAR store_id
        VARCHAR order_date
        VARCHAR order_status
        VARCHAR shipping_amount
        VARCHAR discount_total
        VARCHAR payment_status
        TIMESTAMP _ingested_at
        VARCHAR _source_file
    }

    stg_order_items {
        VARCHAR order_item_id
        VARCHAR order_id
        VARCHAR product_id
        VARCHAR quantity
        VARCHAR unit_price
        VARCHAR discount
        VARCHAR line_total
        TIMESTAMP _ingested_at
        VARCHAR _source_file
    }

    stg_payments {
        VARCHAR payment_id
        VARCHAR order_id
        VARCHAR payment_method
        VARCHAR payment_status
        VARCHAR payment_amount
        VARCHAR payment_date
        VARCHAR transaction_ref
        TIMESTAMP _ingested_at
        VARCHAR _source_file
    }
```
